# 客服身份系统前端实现检查报告

> **检查时间**: 2026-01-23  
> **检查范围**: 客服身份系统的前端实现与后端接口匹配度

---

## 📋 一、检查结果概览

| 功能模块       | 后端接口  | 前端实现    | 匹配状态   | 缺失项       |
| ---------- | ----- | ------- | ------ | --------- |
| **登录/注册**  | ✅ 已实现 | ✅ 已实现   | ✅ 匹配   | -         |
| **我的工单**   | ✅ 已实现 | ⚠️ 部分实现 | ⚠️ 不完整 | API调用参数错误 |
| **待接入会话**  | ❌ 未实现 | ❌ 未实现   | ❌ 缺失   | 页面和接口都缺失  |
| **通知中心**   | ✅ 已实现 | ✅ 已实现   | ✅ 匹配   | -         |
| **我的工作台**  | ❌ 未实现 | ❌ 未实现   | ❌ 缺失   | 页面和接口都缺失  |
| **知识库查询**  | ✅ 已实现 | ❌ 未实现   | ❌ 缺失   | 页面缺失      |
| **个人设置**   | ✅ 已实现 | ❌ 未实现   | ❌ 缺失   | 页面缺失      |
| **在线状态管理** | ✅ 已实现 | ❌ 未实现   | ❌ 缺失   | API调用缺失   |
| **心跳接口**   | ✅ 已实现 | ❌ 未实现   | ❌ 缺失   | API调用缺失   |

---

## 🔍 二、详细问题分析

### 2.1 ✅ 已实现且匹配的功能

#### 1. 登录功能
- **后端**: `POST /api/admin/auth/login`
- **前端**: `Login.tsx` - 已实现角色选择，登录后根据角色跳转
- **状态**: ✅ 完全匹配

#### 2. 注册功能
- **后端**: `POST /api/admin/auth/registerTenant`
- **前端**: `Register.tsx` - 已实现租户注册
- **状态**: ✅ 完全匹配

#### 3. 路由配置
- **后端**: 无直接接口
- **前端**: `App.tsx` - 已配置客服路由 `/agent/tickets`
- **状态**: ✅ 基本匹配（但缺少其他路由）

---

### 2.2 ⚠️ 部分实现但有问题

#### 1. 我的工单页面 (`AgentTicketPage.tsx`)

**问题1: API调用参数错误**
```typescript
// 当前实现（错误）
await ticketApi.transition({
  ticketId,
  targetStatus,
  comment: ''
});

// 后端期望的格式
POST /api/admin/ticket/transition
{
  "ticketId": number,
  "targetStatus": string,
  "comment": string
}
```

**问题2: API调用方法签名不匹配**
```typescript
// frontend/src/api/ticket.ts (当前)
transition(ticketId: number, targetStatus: string, comment?: string) {
  return http.post<ApiResponse<void>>('/admin/ticket/transition', {
    ticketId,
    targetStatus,
    comment
  });
}

// frontend/src/pages/agent/AgentTicketPage.tsx (调用方式)
await ticketApi.transition({
  ticketId,
  targetStatus,
  comment: ''
});
```

**修复方案**:
```typescript
// 方案1: 修改API调用方式
await ticketApi.transition(ticketId, targetStatus, '');

// 方案2: 修改API方法支持对象参数
transition(request: { ticketId: number; targetStatus: string; comment?: string }) {
  return http.post<ApiResponse<void>>('/admin/ticket/transition', request);
}
```

**问题3: 工单列表查询缺少 `assigneeId` 参数**
- 后端接口支持 `assigneeId` 参数（管理员用）
- 前端调用时未传递，但客服场景不需要，可以忽略

---

### 2.3 ❌ 缺失的功能

#### 1. 待接入会话页面 (`/agent/sessions`)

**后端接口**: ❌ 未实现
- 需要实现转人工记录查询接口
- 建议接口: `GET /api/admin/session/pending?page=1&size=10`

**前端页面**: ❌ 未实现
- `App.tsx` 中引用了 `AgentSessionsPage`，但文件不存在
- 需要创建 `frontend/src/pages/agent/AgentSessionsPage.tsx`

**实现建议**:
```typescript
// 后端接口（需要实现）
@GetMapping("/pending")
@RequireRole("AGENT")
public ApiResponse<IPage<TransferRecord>> listPendingSessions(
    @RequestParam(defaultValue = "1") int page,
    @RequestParam(defaultValue = "10") int size
) {
    Long tenantId = SecurityUtil.currentTenantId();
    Long userId = SecurityUtil.currentUserId();
    // 查询分配给当前客服的待接入会话
    return ApiResponse.success("OK", transferService.listPending(tenantId, userId, page, size));
}
```

---

#### 2. 我的工作台页面 (`/agent/workspace`)

**后端接口**: ❌ 未实现
- 需要实现工作台数据聚合接口
- 建议接口: `GET /api/admin/workspace/stats`

**前端页面**: ❌ 未实现
- `AgentLayout.tsx` 中定义了菜单项，但路由未配置
- 需要创建 `frontend/src/pages/agent/AgentWorkspacePage.tsx`

**实现建议**:
```typescript
// 后端接口（需要实现）
@GetMapping("/stats")
@RequireRole("AGENT")
public ApiResponse<WorkspaceStats> getWorkspaceStats() {
    Long tenantId = SecurityUtil.currentTenantId();
    Long userId = SecurityUtil.currentUserId();
    // 返回：当前处理的会话数、工单数、待接入会话数等
    return ApiResponse.success("OK", workspaceService.getStats(tenantId, userId));
}
```

---

#### 3. 知识库查询页面 (`/agent/knowledge`)

**后端接口**: ✅ 已实现
- `GET /api/admin/knowledge/list` (管理员接口，客服也可以复用)

**前端页面**: ❌ 未实现
- `AgentLayout.tsx` 中定义了菜单项，但路由未配置
- 需要创建 `frontend/src/pages/agent/AgentKnowledgePage.tsx`

**实现建议**:
- 复用管理员的 `KnowledgePage.tsx`，但只读模式
- 或者创建简化版的知识库查询页面

---

#### 4. 个人设置页面 (`/agent/profile`)

**后端接口**: ✅ 部分实现
- `PUT /api/admin/user/online-status` - 在线状态更新 ✅
- 个人信息更新接口（需要确认）

**前端页面**: ❌ 未实现
- `AgentLayout.tsx` 中定义了菜单项，但路由未配置
- 需要创建 `frontend/src/pages/agent/AgentProfilePage.tsx`

**实现建议**:
```typescript
// 需要实现的功能：
// 1. 在线状态切换（ONLINE/OFFLINE/AWAY）
// 2. 个人信息查看和编辑
// 3. 密码修改
```

---

#### 5. 在线状态管理 API

**后端接口**: ✅ 已实现
- `PUT /api/admin/user/online-status?status=ONLINE`
- `POST /api/admin/auth/heartbeat`

**前端API调用**: ❌ 未实现
- 需要创建 `frontend/src/api/agent.ts`
- 需要实现心跳定时器

**实现建议**:
```typescript
// frontend/src/api/agent.ts
export const agentApi = {
  // 更新在线状态
  updateOnlineStatus(status: 'ONLINE' | 'OFFLINE' | 'AWAY') {
    return http.put<ApiResponse<void>>('/admin/user/online-status', null, {
      params: { status }
    });
  },
  
  // 心跳
  heartbeat() {
    return http.post<ApiResponse<void>>('/admin/auth/heartbeat');
  }
};
```

---

## 🔧 三、修复方案

### 3.1 立即修复（P0）

#### 1. 修复 `AgentTicketPage.tsx` 的 API 调用

**文件**: `frontend/src/pages/agent/AgentTicketPage.tsx`

**修改**:
```typescript
// 修改前
await ticketApi.transition({
  ticketId,
  targetStatus,
  comment: ''
});

// 修改后
await ticketApi.transition(ticketId, targetStatus, '');
```

**或者修改 `ticketApi.transition` 方法**:
```typescript
// frontend/src/api/ticket.ts
transition(request: { ticketId: number; targetStatus: string; comment?: string }) {
  return http.post<ApiResponse<void>>('/admin/ticket/transition', request);
}
```

#### 2. 修复 `App.tsx` 中的缺失导入

**文件**: `frontend/src/App.tsx`

**问题**: 引用了 `AgentSessionsPage` 但未导入

**修复**:
```typescript
// 方案1: 暂时注释掉，等实现后再启用
// import AgentSessionsPage from './pages/agent/AgentSessionsPage';

// 方案2: 创建占位页面
// 创建 frontend/src/pages/agent/AgentSessionsPage.tsx（临时占位）
```

---

### 3.2 需要实现的功能（P0）

#### 1. 创建待接入会话页面

**文件**: `frontend/src/pages/agent/AgentSessionsPage.tsx`

**功能**:
- 显示分配给当前客服的待接入会话列表
- 支持接入会话操作
- 显示会话详情

**依赖**:
- 后端需要实现转人工记录查询接口

---

#### 2. 实现在线状态管理

**文件**: `frontend/src/api/agent.ts` (新建)

**功能**:
- 在线状态更新 API
- 心跳 API

**文件**: `frontend/src/pages/agent/AgentProfilePage.tsx` (新建)

**功能**:
- 在线状态切换组件
- 心跳定时器（登录后启动，登出后停止）

---

### 3.3 可选实现的功能（P1）

#### 1. 我的工作台页面

**文件**: `frontend/src/pages/agent/AgentWorkspacePage.tsx`

**功能**:
- 显示当前处理的会话数
- 显示当前处理的工单数
- 显示待接入会话数
- 快速操作入口

---

#### 2. 知识库查询页面

**文件**: `frontend/src/pages/agent/AgentKnowledgePage.tsx`

**功能**:
- 知识库文档列表（只读）
- 文档搜索
- 文档详情查看

---

#### 3. 个人设置页面

**文件**: `frontend/src/pages/agent/AgentProfilePage.tsx`

**功能**:
- 在线状态切换
- 个人信息查看和编辑
- 密码修改

---

## 📝 四、修复优先级

### P0（立即修复）
1. ✅ **已修复** `AgentTicketPage.tsx` 的 API 调用错误
2. ✅ **已修复** `App.tsx` 中的缺失导入
3. ✅ **已创建** `AgentSessionsPage.tsx` 占位页面（避免路由错误）
4. ✅ **已创建** `frontend/src/api/agent.ts` 实现在线状态和心跳 API
5. ✅ **已修复** `Login.tsx` 中的角色跳转逻辑

### P1（尽快实现）
1. ✅ 实现待接入会话页面（需要后端接口支持）
2. ✅ 实现在线状态管理（心跳定时器）
3. ✅ 实现个人设置页面

### P2（后续实现）
1. ✅ 实现我的工作台页面
2. ✅ 实现知识库查询页面

---

## 🎯 五、检查清单

### 后端接口检查
- [x] `POST /api/admin/auth/login` - 登录 ✅
- [x] `POST /api/admin/auth/registerTenant` - 注册 ✅
- [x] `GET /api/admin/ticket/list` - 工单列表 ✅
- [x] `POST /api/admin/ticket/transition` - 工单状态流转 ✅
- [x] `PUT /api/admin/user/online-status` - 在线状态更新 ✅
- [x] `POST /api/admin/auth/heartbeat` - 心跳 ✅
- [ ] `GET /api/admin/session/pending` - 待接入会话列表 ❌
- [ ] `GET /api/admin/workspace/stats` - 工作台统计 ❌

### 前端页面检查
- [x] `Login.tsx` - 登录页面 ✅（已修复角色跳转）
- [x] `Register.tsx` - 注册页面 ✅
- [x] `AgentLayout.tsx` - 客服布局 ✅
- [x] `AgentTicketPage.tsx` - 我的工单 ✅（已修复API调用）
- [x] `AgentSessionsPage.tsx` - 待接入会话 ✅（已创建占位页面）
- [ ] `AgentWorkspacePage.tsx` - 我的工作台 ❌
- [ ] `AgentKnowledgePage.tsx` - 知识库查询 ❌
- [ ] `AgentProfilePage.tsx` - 个人设置 ❌

### 前端API检查
- [x] `ticketApi.list` - 工单列表 ✅
- [x] `ticketApi.transition` - 工单状态流转 ✅（已修复）
- [x] `agentApi.updateOnlineStatus` - 在线状态更新 ✅（已创建）
- [x] `agentApi.heartbeat` - 心跳 ✅（已创建）

### 路由配置检查
- [x] `/agent/tickets` - 我的工单 ✅
- [x] `/agent/sessions` - 待接入会话 ✅（已配置，占位页面）
- [ ] `/agent/workspace` - 我的工作台 ❌（未配置）
- [ ] `/agent/knowledge` - 知识库查询 ❌（未配置）
- [ ] `/agent/notify` - 通知中心 ❌（未配置）
- [ ] `/agent/profile` - 个人设置 ❌（未配置）

---

## 📚 六、总结

### 当前状态
- ✅ **基础功能已实现**: 登录、注册、工单列表查看、工单状态流转
- ✅ **已修复问题**: API调用格式错误、缺失导入、角色跳转逻辑
- ✅ **已创建基础文件**: 待接入会话占位页面、在线状态API
- ❌ **待实现功能**: 待接入会话完整实现、工作台、知识库、个人设置、在线状态管理UI

### 建议
1. ✅ **已完成** API调用错误修复，现有功能可用
2. **优先实现** 待接入会话页面完整功能（需要后端接口支持）
3. **优先实现** 在线状态管理UI（心跳定时器、状态切换组件）
4. **逐步完善** 其他功能模块（工作台、知识库、个人设置）

---

**文档维护**: 请根据修复进展及时更新此文档  
**最后更新**: 2026-01-23

